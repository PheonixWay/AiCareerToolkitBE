version: '3.8'

services:
  backend-api:
    build: .
    container_name: aicareer_backend
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    restart: always # Agar server reboot hua toh yeh app apne aap start ho jayega